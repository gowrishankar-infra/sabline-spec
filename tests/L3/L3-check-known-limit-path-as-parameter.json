{
  "id": "L3-check-known-limit-path-as-parameter",
  "level": 3,
  "kind": "check",
  "description": "a path passed to a helper as a parameter is taken as unscoped, even when every caller passes a literal",
  "spec": [
    "9.3"
  ],
  "from": "check_ratchet.py CHECKS known-limit-path-as-parameter",
  "requires": [],
  "known_limit": "A change that does not widen, reported as one. The program reads data/in.csv as before, but a parameter is not fixed text (velaris-spec 9.3 step 4), so the grant it needs is fs:read, any path.",
  "input": {
    "tree": {
      "read.vel": "fn main() uses io, fs {\n    check read_file(\"data/in.csv\") {\n        ok text {\n            print(text)\n        }\n        fail why {\n            print(why)\n        }\n    }\n}\n"
    },
    "root": ".",
    "baseline": {
      "from_tree": true,
      "edit": null
    },
    "change": {
      "read.vel": "fn load(path: Text) -> Text uses fs or fail {\n    return try read_file(path)\n}\n\nfn main() uses io, fs {\n    check load(\"data/in.csv\") {\n        ok text {\n            print(text)\n        }\n        fail why {\n            print(why)\n        }\n    }\n}\n"
    }
  },
  "expect": {
    "verdict": "widened",
    "widenings": [
      {
        "kind": "grant",
        "grant": "fs:read",
        "rules": [
          "W1",
          "W3"
        ],
        "programs": [
          "read.vel"
        ]
      }
    ]
  }
}
