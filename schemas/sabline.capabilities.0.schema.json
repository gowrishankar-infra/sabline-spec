{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://raw.githubusercontent.com/gowrishankar-infra/sabline-spec/v0.5/schemas/sabline.capabilities.0.schema.json",
  "title": "sabline.capabilities/0 (superseded)",
  "description": "SUPERSEDED in sabline-spec 0.3 by sabline.capabilities/1 (schemas/sabline.capabilities.1.schema.json, SPEC.md section 9). This was the provisional form versions 0.1 and 0.2 defined: for each program in a repository, the grants its sabline.audit/1 document says it needs. No version of the reference implementation wrote or read it, and the reference refuses to compare against one. It is kept so that documents written against 0.2 can still be recognised.",
  "type": "object",
  "required": [
    "schema",
    "programs"
  ],
  "additionalProperties": false,
  "properties": {
    "schema": {
      "const": "sabline.capabilities/0"
    },
    "generated_by": {
      "description": "Informative: what wrote this document.",
      "type": "string"
    },
    "programs": {
      "type": "array",
      "items": {
        "$ref": "#/$defs/entry"
      }
    }
  },
  "$defs": {
    "entry": {
      "type": "object",
      "required": [
        "file",
        "grants"
      ],
      "additionalProperties": false,
      "properties": {
        "file": {
          "description": "The program's entry file, relative to the directory that holds the baseline, '/'-separated, with no empty, '.' or '..' component.",
          "type": "string",
          "pattern": "^(?!/)(?!.*//)(?!.*/$)(?!(?:.*/)?\\.{1,2}(?:/|$))[^\\\\]+$"
        },
        "grants": {
          "description": "The grants the program's audit says it needs, in the budget grammar of SPEC.md section 4 with the restrictions of section 9.2: no counts, one module per ffi: grant, paths as the program writes them.",
          "type": "array",
          "uniqueItems": true,
          "items": {
            "$ref": "#/$defs/grant"
          }
        },
        "note": {
          "description": "For people. Tools must not read meaning into it.",
          "type": "string"
        }
      }
    },
    "grant": {
      "anyOf": [
        {
          "description": "An effect, unscoped.",
          "enum": [
            "io",
            "env",
            "clock",
            "rand",
            "ffi",
            "fs",
            "net"
          ]
        },
        {
          "description": "One Python module: text up to the first '.', no ',', '@', ':' or whitespace.",
          "type": "string",
          "pattern": "^ffi:[^,@.:\\s]+$"
        },
        {
          "description": "One fs direction, any path.",
          "enum": [
            "fs:read",
            "fs:write"
          ]
        },
        {
          "description": "One fs direction under a path, written as the program writes it; no ',' or '@'.",
          "type": "string",
          "pattern": "^fs:(read|write):[^,@]+$"
        },
        {
          "description": "A host name, lower case, optionally with a port.",
          "type": "string",
          "pattern": "^net:[^,@/*\\s:\\[\\]A-Z]+(:[0-9]{1,5})?$"
        },
        {
          "description": "A wildcard: '*.' and at least two labels, optionally with a port.",
          "type": "string",
          "pattern": "^net:\\*\\.[^,@/*\\s:\\[\\]A-Z.]+(\\.[^,@/*\\s:\\[\\]A-Z.]+)+(:[0-9]{1,5})?$"
        },
        {
          "description": "An IPv6 address in brackets, optionally with a port.",
          "type": "string",
          "pattern": "^net:\\[[0-9a-f:.]+\\](:[0-9]{1,5})?$"
        }
      ]
    }
  }
}
